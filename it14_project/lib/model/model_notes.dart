class Note {
  String id;
  String subtitle;
  String title;
  String time;
  String image; // Change the type to String for the image URL
  bool isDon;

  Note(this.id, this.subtitle, this.time, this.image, this.title, this.isDon);
}
